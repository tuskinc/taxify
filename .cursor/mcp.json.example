{
  "mcpServers": {
    "supabase": {
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "@supabase/mcp-server-supabase",
        "--access-token",
        "YOUR_SUPABASE_ACCESS_TOKEN_HERE"
      ]
    }
  }
}

NOTE: Replace YOUR_SUPABASE_ACCESS_TOKEN_HERE with your actual Supabase access token.
This file should never be committed to version control.

